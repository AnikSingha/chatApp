import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    type: 'dark',
    primary: {
      main: '#343541',
    },
    secondary: {
      main: '#4041ff',
    },
  },
});

export default theme;